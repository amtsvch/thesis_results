public SocketWrapperBase(E socket, AbstractEndpoint&lt;E, ?&gt; endpoint) {                            public SocketWrapperBase(E socket, AbstractEndpoint&lt;E, ?&gt; endpoint) {                         
this.socket = socket;                                                                                  this.socket = socket;                                                                               
this.endpoint = endpoint;                                                                              this.endpoint = endpoint;                                                                           
if (endpoint.getUseAsyncIO() || needSemaphores()) {                                                    if (endpoint.getUseAsyncIO() || needSemaphores()) {                                                 
readPending = new Semaphore(1);                                                                        readPending = new Semaphore(1);                                                                     
writePending = new Semaphore(1);                                                                       writePending = new Semaphore(1);                                                                    
} else {                                                                                               } else {                                                                                            
readPending = null;                                                                                    readPending = null;                                                                                 
writePending = null;                                                                                   writePending = null;                                                                                
}                                                                                                      }                                                                                                   
                                                                                                     | connectionId = Long.toString(connectionIdGenerator.getAndIncrement());                              
}                                                                                                      }                                                                                                   
